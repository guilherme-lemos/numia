# Unit tests

Isolated, fast, no real files/DB/network. Mock or fake anything external.
Mirror the `src/numia/` module layout (`test_<module>.py`). Counts toward the
90% coverage floor — see `AGENTS.md` § Testing strategy.
